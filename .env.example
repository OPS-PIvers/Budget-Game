# Example environment variables for Budget Game MCP Server
# Copy this to .env and fill in your actual values

# Option 1: Path to service account JSON file
SERVICE_ACCOUNT_PATH="/home/codespace/.config/budget-game-mcp/service-account.json"

# Option 2: Base64 encoded service account JSON (alternative to file path)
# CREDENTIALS_CONFIG="<base64-encoded-json-content>"

# Your Google Drive folder ID containing the Budget Game sheets
DRIVE_FOLDER_ID="your-drive-folder-id-here"

# Your Google Sheet ID (from the URL)
GOOGLE_SHEET_ID="your-sheet-id-here"